package Question2_0_String_Manipulation.Question2_12_ReverseStringWithSpacesPosition;

public class Note {

// Assumption, if I don't the assumption then I could not work it out. there is always a space in between
// It is actually very hard to read codes. if I don't run it. I could not get the following code



/*    while (j >= 0 && resultArray[j] == ' ') {
        j--;//What if that the j is also empty?
    }
    if (j >= 0) {
        resultArray[j] = inputStringArray[i];
        j--;
    }*/




    //I Am Not String
    //g ni rtS toNmAI


//


}