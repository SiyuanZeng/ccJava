package Question2_0_String_Manipulation.Question2_1_DuplicateWordsInString;

/**
 * Created by SiyuanZeng's on 4/24/2016.
 */
public class NoteNote {

// necessary test, no extra unpurpose data



/*
************Next*****************
        if (node  != null){
            table.put (node.data, true);
        }
        while(node.next != null){
            if (table.containsKey (node.next.data)){
                node.next = node.next.next;
            }else{
                table.put (node.next.data, true);
                node = node.next;
            }
            //  node = node.next;// I skip one
        }

* */











}
